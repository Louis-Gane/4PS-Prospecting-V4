{
  "name": "ch-turnover-pipeline",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y poppler-utils tesseract-ocr && pip install -r requirements.txt"
}
